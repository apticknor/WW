
    <div class="tier tier_accent1">
        <div class="wrapper">
            <div role="contentinfo">
                <ul>
                {% for link in linklists.footer.links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
                </ul>
            </div>
        </div>
    </div>

